// Preload script — reserved for future IPC bridge if needed.
// Currently, Clarity operates entirely in the renderer with web APIs.
// Add contextBridge.exposeInMainWorld() here for native file dialogs, etc.
